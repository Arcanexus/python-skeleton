[tool.pytest.ini_options]
addopts = "-ra -v --cov=src --cov-report json --cov-report html --cov-report term "
pythonpath = [
  ".", "src",
]
required_plugins = [
  "pytest-cov==7.1.0",
  "pytest-mock==3.15.1"
]

[tool.coverage.paths]
source = [
    "src/"
    ]
